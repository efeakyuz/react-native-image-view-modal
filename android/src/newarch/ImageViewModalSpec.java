package com.imageviewmodal;

import com.facebook.react.bridge.ReactApplicationContext;

abstract class ImageViewModalSpec extends NativeImageViewModalSpec {
  ImageViewModalSpec(ReactApplicationContext context) {
    super(context);
  }
}
